#ifndef MY_HELPERFUNCTION
#define MY_HELPERFUNCTION

#include <vector>

using namespace std;

void creatingVector(vector<vector<int> >&);
void clear(vector<vector<int> >&);
int compare(vector<int>, int, int, int);
void line();
void menu();
void print(vector<vector<int> >);
void swap(vector<int>&, int, int);
void theWhole(vector<vector<int> >&);


#endif
